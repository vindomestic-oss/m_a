TODO — suggestions for article improvement
==========================================

1. ЗАПОЛНИТЬ УЖЕ ОБЕЩАННОЕ

- §6.3 [TODO]: нужно число enrichment без inversion merging.
  Запустить meta_analysis.py с --no-inv или взять из старых данных.
  Без этого §6.3 выглядит незаконченным.

- Таблица 2: в §6.2 упоминается, что shift−1 отношения "systematically larger"
  чем shift+1, но в таблице только shift+1 колонка.
  Нужно добавить shift−1 колонку.

- "Notes for revision" внизу article.md: убрать перед отправкой в журнал.


2. ИСПРАВИТЬ НЕСООТВЕТСТВИЕ В ЧИСЛАХ

§5.1 говорит "Bach (full corpus, 1,024 files)",
но §5.2 говорит "Bach (all): 413 files".
Скорее всего §5.1 должен быть "Full corpus, 1,024 files" или "Bach corpus, 413 files".


3. ДОБАВИТЬ КОЛОНКУ ПО ЖАНРАМ В TABLE 3

Разбивка "fugues vs. preludes/dances" для баховского корпуса.
Если фуги показывают более высокий коэффициент, чем прелюдии/танцы,
это поддерживает гипотезу о сознательном использовании
(в фуге мотив управляет формой жёстче).


4. ОБЪЯСНИТЬ КОЛИЧЕСТВЕННО, ПОЧЕМУ СТРУКТУРНОЕ ОБЪЯСНЕНИЕ НЕ РАБОТАЕТ

§6.1 отвергает объяснение через структуру формально, но без числа.
Сильнее было бы: "Если бы плавность унаследовалась от гладкого числа тактов,
мы ожидали бы enrichment ≈ X×. Наблюдаемое 2.25× превосходит это на Y×."
Это закрывает самое очевидное возражение рецензента.


5. ОПИСАТЬ ФОРМАЛЬНЫЕ РАЗДЕЛЫ В §4

BWV 852: "три раздела" названы, но не очерчены тактами.
Одна строка — "first section bars 1–N, middle bars N–M, final bars M–end" —
делает пример проверяемым.


6. ДОБАВИТЬ ОТРИЦАТЕЛЬНЫЙ ПРИМЕР

Одна пьеса с заметно негладким числом (17 или 22 вхождения главной фигуры) —
это не опровержение, а честность.
Можно в сноске: "BWV X: главный мотив встречается 17 раз;
такие случаи составляют ≈58% от корпуса."


7. РАЗНИЦА BEETHOVEN CORPUS (1.72×) VS. OPUS 132 (0.31×)

Opus 132 показывает 0.31× — резкий выброс вниз.
Opus 132 — позднее творчество, нерегулярное по метру.
Стоит либо упомянуть в §5.2 как исключение,
либо проверить, не ошибка ли это в расчёте.


8. ПОЯСНИТЬ BIAS "MAXIMUM 50 PER PIECE"

§3.2 говорит "up to 50 per piece are collected".
Рецензент спросит: не создаёт ли это bias в сторону высоких счётчиков?
Ответ: да, намеренно (эффект ожидается там сильнее) — сказать явно.


ПРИОРИТЕТЫ:
  * Исправить несоответствие §5.1/§5.2 (фактическая ошибка)
  * Добавить shift−1 в таблицу 2 (обещана в тексте)
  * Заполнить [TODO] в §6.3 (no-inv enrichment число)
